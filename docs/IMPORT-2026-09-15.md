# Supplied archive import

Archive: `powerfarm-continuity-v2-self-hosting (1).zip`

SHA256: `02d540c2d47c8dca1b1a8a2f02110db1ee250ba782daa370715240146180aaa5`

Initial import commit `ccb519a` preserves all 114 files byte for byte. Empty directories remain in the supplied ZIP because Git does not store them. The complete Go suite and supplied graph/act verification passed locally.

This source publication does not replace the LangGraph adapter currently running in Antenna. Temporal worker and real MCP effect dispatch remain incomplete as recorded in STATUS.md.
