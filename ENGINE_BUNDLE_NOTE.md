# Engine bundle note

This compact source package intentionally excludes the large third-party source trees and compiled industrial libraries. Pair it with the previously downloaded `continuity-engines.tar.gz` when rebuilding the complete workspace.

The POWERFARM-owned code, engine lockfiles/fetchers, Open Workflow schema metadata, policy, integration tests, SQLite effect journal, and industrial demo source are included.
